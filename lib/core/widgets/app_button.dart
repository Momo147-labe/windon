// (sheet as dynamic).appendRow(widget.columns.map((c) => c.title).toList());

//     for (var e in filtered) {
//       (sheet as dynamic).appendRow(
//         widget.columns.map((c) => widget.searchBy(e)).toList(),
//       );
//     }